name: fasse_front
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  logger: ^2.7.0
  intl: ^0.20.3
  flutter_secure_storage: ^10.3.1
  flutter_web_auth_2: ^5.0.3
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/foodcompositions.zip
    - .env
