# API Gateway base URL for the purchase-sales backend
API_BASE_URL=https://<api-id>.execute-api.<region>.amazonaws.com/stg

# AccessKey for local development (route A). Leave empty/unset to force the Cognito login flow.
# Do not put a real AccessKey in a build meant for stg (see docs/spec/purchase-sales-frontend/design.md).
ACCESS_KEY=

# Cognito Hosted UI settings (route B). Provided by fasse_infra once Phase 2 (Cognito) is set up.
COGNITO_DOMAIN=https://<prefix>.auth.<region>.amazoncognito.com
COGNITO_CLIENT_ID=<cognito-app-client-id>

# Fixed origin hosting auth_callback.html (e.g. the stg deployment domain). Only this ONE URL needs
# to be registered as a Cognito App Client callback URL, for both stg builds and local `flutter run`
# (regardless of --web-port), via FlutterWebAuth2Options.debugOrigin.
# Leave unset to fall back to the running origin (requires a fixed --web-port and a matching
# per-developer callback URL registered in Cognito for local testing).
COGNITO_CALLBACK_ORIGIN=https://<stg配信ドメイン>
